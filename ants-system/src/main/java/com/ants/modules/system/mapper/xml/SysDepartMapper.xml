<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ants.modules.system.mapper.SysDepartMapper">

    <!-- 根据username查询所拥有的部门 -->
    <select id="queryDepartsByUsername" parameterType="String" resultType="com.ants.modules.system.entity.SysDepart">
        SELECT *
        FROM sys_depart
        WHERE id IN (
            SELECT dep_id
            FROM sys_user_depart
            WHERE user_id = (
                SELECT id
                FROM sys_user
                WHERE username = #{username}
            )
        )
    </select>

    <!--通过部门id查询部门编码 -->
    <select id="getDepartCodesByDeptIds" resultType="java.lang.String">
        select org_code from sys_depart where del_flag = '0' and id in
        <foreach collection="deptIds" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        order by org_code asc
    </select>

    <!--通过用户名查询部门编码 -->
    <select id="getDepartCodesByUserNames" resultType="java.lang.String">
        select org_code from sys_depart where del_flag='0' and id in
        (
        select distinct dep_id from sys_user_depart where user_id in
        (
        select id from sys_user where del_flag = '0' and username in
        <foreach collection="userNames" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
        )
        )

    </select>
</mapper>
