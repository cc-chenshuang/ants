<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ants.modules.ArticleManage.mapper.ArticleLikeCollectionMapper">


    <select id="myFavorites" resultType="java.util.Map" parameterType="string">
        SELECT
            LEFT ( a.create_time, 10 ) createTime,
            b.title,
            b.html,
            b.id
        FROM
            article_like_collection a
                LEFT JOIN article_manage b ON a.article_id = b.id
        WHERE
            a.type = '1'
          and b.del_flag = 0
          AND a.username = #{username}
        ORDER BY
            a.create_time DESC
    </select>
</mapper>
